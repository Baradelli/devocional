# delivery

Borda HTTP (Fastify): rotas, validação com Zod, serialização. Camada fina —
não contém regra de negócio. Chama use-cases da camada `application`.

M0: `server.ts` (fábrica do servidor) + `routes/health.ts`.

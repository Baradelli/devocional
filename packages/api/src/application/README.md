# application

Use-cases, um por operação. Orquestram o domínio e dependem de **portas**
(interfaces) implementadas pela infraestrutura. Unidade natural de teste.

Vazio no M0: o primeiro use-case (`completeDevotional`) chega no M2.
